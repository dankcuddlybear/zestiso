# Maintainer: Dankcuddlybear <dankcuddlybear@proton.me>
pkgname=zestiso-core
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='ZestISO minimal core installation'
arch=(any)
url='https://github.com/dankcuddlybear/zestiso'
depends=(
	arch-tweaks
	base
	bashrc-d
	bundle-networking
	efibootmgr
	linux-firmware
	sudo
	zestiso-branding
)
