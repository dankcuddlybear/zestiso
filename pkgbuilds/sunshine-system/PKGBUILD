# Maintainer: Dankcuddlybear <dankcuddlybear@proton.me>
pkgname=sunshine-system
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='Run Sunshine GameStream host for Moonlight at system level, allows remote login in Moonlight client'
arch=(any)
url='https://github.com/dankcuddlybear/zestiso'
install=install.install
depends=(sunshine)
package() {
	cp -r "$srcdir/../files/usr" "$pkgdir"
	"$srcdir/../../set-default-permissions.sh" "$pkgdir"
}
